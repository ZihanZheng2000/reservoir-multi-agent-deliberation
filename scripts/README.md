# Scripts

This directory contains reproducibility scripts for report assets.

## Available Scripts

- `generate_report_figures.py`: regenerates the current report figures from the score arrays embedded in the script.

Run from the repository root:

```powershell
python scripts/generate_report_figures.py
```
