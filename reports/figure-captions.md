# Figure Captions

## Figure 1. Role-limited multi-agent reservoir approval deliberation, agent-facing view

Seven expert and stakeholder roles deliberate on a proposed reservoir using shared scenario materials. The hidden evaluator checklist is not shown to agents and is applied only after generation for scoring. The figure separates agent-facing deliberation from post-generation scoring, cross-model robustness checking, and the three-layer evaluation structure.

## Figure 2. Multi-agent advantage over single-agent and MCDA baselines

Each row compares multi-agent, single-agent, and MCDA systems on the same case using the directly comparable layers: Outcome Quality (L1) plus Evidence Quality (L2), with a maximum score of 70. Right-side labels report the multi-agent gain over single-agent and MCDA baselines. The figure shows that the multi-agent advantage is largest in high-conflict, governance-heavy, emergency-sequencing, and tipping-point cases, while the gap is smallest in the clean approval case.

## Figure 3. Case-level diagnostic profile of multi-agent deliberation

The heatmap separates five diagnostic dimensions for each case and model environment. Outcome, Evidence, and Process columns report normalized multi-agent layer scores. Gain columns report additional L1+L2 points over single-agent and MCDA baselines. Cell color is column-normalized to reveal high and low cases within each diagnostic dimension. This figure is intended to support case-level interpretation rather than a single aggregate performance claim.
